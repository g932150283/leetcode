#include<bits/stdc++.h>

using namespace std;

int main(){

    pair<vector<vector<int> >, vector<vector<int> > > res;
    res.first.push_back({0, 1});
    res.second.push_back({0, 1});

    res.first.push_back({1, 1});
    res.second.push_back({1, 0});

    res.first.push_back({1, 2});
    res.second.push_back({1, 1});

    // vector<pair<int,int> > ::iterator iter;
    // for(iter=vec.begin();iter!=vec.end();iter++);





    return 0;
}